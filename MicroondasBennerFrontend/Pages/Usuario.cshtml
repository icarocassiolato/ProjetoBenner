@page

<h2>Cadastro de Usuários</h2>

<button onclick="window.location.href='/'">Voltar</button>

<hr>

<h3>Novo Usuário</h3>

<div>
    <input id="nome" placeholder="Nome" />
</div>

<div>
    <input id="senha" type="password" placeholder="Senha" />
</div>

<br>

<button onclick="salvarUsuario()">Salvar</button>

<hr>

<h3>Usuários Cadastrados</h3>

<table border="1" width="100%">
    <thead>
        <tr>
            <th>Nome</th>
            <th>Senha</th>
            <th>Ações</th>
        </tr>
    </thead>

    <tbody id="listaUsuarios"></tbody>
</table>

<script>

    let token = null;
    let editandoId = null;

    async function carregarUsuarios(){
        const usuarios = await sendRequest("usuario", "GET");

        const tabela = document.getElementById("listaUsuarios");
        tabela.innerHTML = "";

        usuarios.forEach(u => {
            const linha = document.createElement("tr");

            linha.innerHTML = `
                <td>${u.nome}</td>
                <td>******</td>
                <td>
                    <button onclick="editarUsuario(${u.id},'${u.nome}')">Editar</button>
                    <button onclick="excluirUsuario(${u.id})">Excluir</button>
                </td>
            `;

            tabela.appendChild(linha);
        });
    }

    function editarUsuario(id,nome){

        editandoId = id;

        document.getElementById("nome").value = nome;
        document.getElementById("senha").value = "";
    }

    async function salvarUsuario(){
        const usuario = {
            nome: document.getElementById("nome").value,
            senha: document.getElementById("senha").value
        };

        if(editandoId){
            usuario.id = editandoId;

            await sendRequest("usuario", "PUT", usuario);

            editandoId = null;

        }else{
            await sendRequest("usuario", "POST", usuario);
        }

        limparCampos();
        carregarUsuarios();
    }

    async function excluirUsuario(id){

        if(!confirm("Excluir usuário?"))
            return;

        await sendRequest(`usuario/${id}`, "DELETE");

        carregarUsuarios();
    }

    function limparCampos(){

        document.getElementById("nome").value="";
        document.getElementById("senha").value="";
    }

    async function inicializar(){
        await carregarUsuarios();
    }

    window.onload = inicializar;
</script>